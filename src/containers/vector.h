#pragma once

/// \file vector.h
///
/// A custom implementation of a dynamically re-sizable array
/// with templated value type.

/// \class Vector
///
/// \tparam ValueT The type of each element.
template<typename ValueT>
class Vector
{};
